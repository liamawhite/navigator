# API Configuration
# For development with Vite proxy (recommended - avoids CORS issues):
VITE_API_URL=

# For direct API access (requires backend CORS support):
# VITE_API_URL=http://localhost:8081

# For production (served from same origin):
# VITE_API_URL=/api