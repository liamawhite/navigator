apiVersion: v1
kind: Service
metadata:
  name: monolith
  namespace: monolith
  labels:
    app.kubernetes.io/name: monolith
    app.kubernetes.io/component: single-tier
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: monolith